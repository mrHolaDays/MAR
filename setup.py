from setuptools import setup, find_packages

setup(
    name="marlib",
    version="1.0.0",
    packages=find_packages(),
    install_requires=[],
    description="MAR Database Library",
    keywords="database mar",
)